#include<stdio.h>
int main()
{

char *names;
names[0]='a';
names[1]='b';
names[2]='c';
printf("%d",names[0]);//编译器warning，未初始化names[0],编译后打印出a的ASCII值
int i=name[3]+(name[2]<<8)+(name[1]<<16)+(name[0]<<24);//一个char类型变量是一个字节8bit，而一个int类型是4个字节32bit，所以可以用一个int类型来表示4个char数组
prinf("name:%d\n",i);
 return 0;
 }
